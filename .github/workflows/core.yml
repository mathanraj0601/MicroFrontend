name: deploy-core

on:
  push:
    branches:
      - develop
    paths:
      - Framework/core/**

defaults:
  run:
    working-directory: Framework/core

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - run: npm install
      - run: npm run build

      # - name: "Azure login"
      #   uses: azure/login@v1
      #   with:
      #     creds: ${{ secrets.AZURE_CREDENTIALS }}
  
      # - uses: bacongobbler/azure-blob-storage-upload@main
      #   with:
      #     source_dir: dist
      #     container_name: $web
      #     connection_string: ${{ secrets.AZURE_CONNECTION_STRING }}
      #     sync: 'true'

      # az copy not working
      # - name: "Blob upload"
      #   uses: azure/cli@v2
      #   with:
      #     azcliversion: latest
      #     inlineScript: |
      #      az storage blob sync "dist" {{ secret.AZURE_BLOB_SAS_URL }}

      # Az storage blob sync not workint
      - name: "Blob upload"
        uses: azure/CLI@v1
        with:
          inlineScript: |
            az storage blob sync --container $web --account-name {{ secrets.AZURE_ACCOUNT_NAME }} --account-key {{ secrets.AZURE_ACCOUNT_KEY }} --source dist



